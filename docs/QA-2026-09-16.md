# Language Relay · QA 2026-09-16 · 2.3.8 → 2.3.9 (build 15)

Wave 5 of the AIM apps sprint (`lab-sites/internal-sites/aim-product-system/SPRINT-2026-09-16-TESTBED-NOTIFY.md`, section B for Relay): one appear transition from the shared tokens, transient panel with a pin, every close path the same.

Method (rule 27). No synthetic click, no `activate`, no cursor movement: the installed build (`release-app.mjs relay --install` → `install-runtime.sh update-background`, rollback snapshot `~/Library/Application Support/Language Relay/Rollback/20260916T09*`) presents the panel through `LanguageRelay --testbed show|hide|pin|unpin`; the tree, the button presses and the window images come from `testbed/bin/axdrive` (`tree`, `press`, `capture`, `wait`, `probe`); headless checks through `make test` (`--self-test` + `--ui-self-test`), `make design-check`, `--render-ui`, `--design-json`, `--key-loop`. Screenshots: `/tmp/codex-screenshots/apps-testbed/relay-239-*.png`. The user worked in Telegram during the run: `probe` reports frontmost Telegram before and after every pass, the cursor position changed only by the user's own movements.

| # | zone | check | expected | result | evidence |
|---|------|-------|----------|--------|----------|
| 1 | tokens | N1 exports `2026.09.16-n1` vendored byte for byte (`AIMMiniAppTokens.swift`, new `AIMHintCard.swift`), receipt hashes match | `make design-check` PASS, 13 artifacts | pass | `sync-design-tokens.mjs --check` |
| 2 | tokens | `AIMHintCard.swift` compiles inside the app (typecheck) | `make build` clean | pass | Makefile `VOXEL_SOURCES` |
| 3 | appear | `motion-panel-appear` 200 ms → `NSPopover.animates`, `motion-window-appear` 180 ms, shift 6 pt exposed by `RelayStyle` | `--design-json` `panelAppear 0.2`, `windowAppear 0.18`, `windowAppearShift 6` | pass | design json, ui-self-test |
| 4 | appear | Reduce Motion → every appear value 0, finished frame at once | self-test with `reducedMotion: true` | pass | ui-self-test (system setting untouched) |
| 5 | appear | testbed presentation takes no transition (first captured frame is the finished panel) | `animates = false` on the agent path | pass | `relay-239-testbed.png` 1 s after `show` |
| 6 | header | live mark 40 pt · name · version · `settings` · pin `◉/○` · `×` on one line, `×` on the 16 pt grid | rule 21 + 29 | pass | AX tree: `showSettingsMenu:` 80×30, `pin-panel` 30×30, `close-panel` 30×30 on one row; ui-self-test |
| 7 | pin | default off: panel transient, button shows `○`, `dev.alex.layout-pilot.pinned` absent | `--design-json` `pinDefault false`, `pinned false` | pass | design json, `relay-239-offscreen.png` |
| 8 | pin | `press "|pin-panel"` → `◉`, red active bar, defaults `1`; press again → `○`, defaults `0` | toggles and persists | pass | AX press ×2, `defaults read`, `relay-239-testbed-pinned.png` |
| 9 | pin | VoiceOver name `pin panel open`, state via AXSelected, help text names the outside-click behaviour | same wording as MEM PRISM | pass | AX tree `desc="pin panel open"` |
| 10 | close | outside click on an unpinned panel (non-activating testbed presentation) | panel closes | pass | the user's own click in Telegram closed the transient panel within 1 s of `show` on three runs; a read-only global monitor probe counted the click; a pinned panel survived the same activity |
| 11 | close | `×` (`press "|close-panel"`) | panel gone, anchor gone | pass | `wait gone: ok`, `windows --app` empty |
| 12 | close | `--testbed hide` route | panel gone | pass | `wait gone: ok` |
| 13 | close | Escape / Command-W on the root view | `closeAction` once, plain `w` and shift-command-w ignored | pass (self-test) | ui-self-test `performKeyEquivalent`; live Escape not reachable on the testbed (window never key, README «known limits») |
| 14 | close | status item click while open | closes through `performClose` | code path | `statusItemAction`, unchanged since 2.3.8 |
| 15 | close | every path ends in `popoverDidClose`: monitor removed, pin button reference dropped, anchor hidden | one close path | pass | `windows --app` shows neither the panel nor the 2 pt anchor after each close |
| 16 | pin | `--testbed pin|unpin` routes toggle the same state as the button | defaults follow | pass | `defaults read` 1 → 0 |
| 17 | focus | Tab order includes the pin between `settings` and `×` | `--key-loop` | pass | `tab 3: RelayButton#pin-panel` |
| 18 | testbed | frontmost app before and after every pass unchanged, no AIM app took focus | `probe` | pass | Telegram → Telegram (cursor moved by the user) |
| 19 | release | `Info.plist` 2.3.9 build 15, `make test`, `make design-check`, `release-app.mjs relay --install`, zip + SHA + notes, page 2.3.8 → 2.3.9, catalog card 2.3.9 | one pass | pass | `sites/apps/language-relay/Language-Relay-2.3.9-arm64.zip`, `release/Language-Relay-v2.3.9-macos.zip` + `.sha256` |
| 20 | page | hero screenshot of the 2.3.9 panel from `axdrive capture` (892×1030, 265 KB), `?v=239`, what's new from `release-notes.txt` | page updated | pass | `sites/apps/language-relay/assets/window.png`, mirrored to `docs/` |

Not verified live: a real outside click by the agent (forbidden on the working Mac, VM stand only), Escape and Command-W on the shown panel (the testbed window never becomes key), the status-item click. These rest on the self-test and unchanged 2.3.8 code paths.

Changes in 2.3.9: `RelayStyle.panelAppearDuration / windowAppearDuration / windowAppearShift`, `AppDelegate.pinned / togglePin / decoratePinButton`, `installOutsideClickMonitor` (global mouse-down monitor, read only), `RelayButton.setLabel`, testbed actions `pin|unpin`, ui-self-test rows for the tokens and the pin, `AIMHintCard.swift` in `VOXEL_SOURCES`.
