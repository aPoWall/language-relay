# QA · Language Relay 2.4.0 build 16 · 2026-09-16

Wave 6 (`SPRINT-2026-09-16-UNIFY.md` § C). Everything ran in the background: offscreen render, the app's own
`--testbed` route and `axdrive`. No synthetic click, no `activate`, the cursor was read and never moved.

| # | check | how | result |
|---|---|---|---|
| 1 | three mark variants, one choice | `docs/design/relay-mark-contacts.svg` rendered at 48 / 24 / 18 next to the four sibling marks | arch chosen, reasons in `docs/design/relay-mark-2.4.0.md` |
| 2 | new symbol id (rule 4) | `aim-app-marks.svg` gains `relay-arch`, `relay` keeps the same paths as an alias | pass |
| 3 | character under the new mark | `build-voxels.mjs --models --swift` | relay 80 voxels, one red signal, inside 10 x 10 x 10 |
| 4 | exports and receipt | `build-mini-apps.mjs`, `check.mjs` | `PASS · 13 N1 exports · swiftc typecheck` |
| 5 | vendored copies | `make design-check` (`sync-design-tokens.mjs --check`) | `PASS: N1 2026.09.16-n1; 13 verified artifacts` |
| 6 | app tests | `make test` | 16 conversion tests, UI self-test pass |
| 7 | menu bar glyph (rule 26) | UI self-test: `RelayMarkGlyph.mark(size: 18)` template 18 x 18, status image 54 x 18 | pass |
| 8 | pin migration (rule 29) | UI self-test on a throwaway defaults suite: on → off once, a later choice survives | pass |
| 9 | gesture | UI self-test: click mirrors the model on `x` and a second click restores it | pass |
| 10 | offscreen render | `--render-ui` | header, pin `○`, footer `v2.4.0` |
| 11 | testbed pass (rule 27) | `--testbed show` → `axdrive windows/tree/capture` → `--testbed hide` → `wait gone` | window 6101 at 1055,443 446x515, 40 AX nodes, frontmost Zoom → Zoom, cursor 830.18,68.98 unchanged |
| 12 | install | `install-runtime.sh preflight` then `update-background` | installed 2.4.0 build 16, rollback snapshot `20260916T173916Z` |
| 13 | release | `release-app.mjs relay --note …` | `Language-Relay-2.4.0-arm64.zip`, 590 KB, sha256 `e80c292a…e29f62eb`, adhoc signature |
| 14 | page | version 22 x, sha, date, `?v=240` hero from the testbed capture, social card and favicon redrawn, `#relay-arch` | pass |
| 15 | text | no U+2014 in the page, the changelog, the README or the new docs | pass |

Screenshots: `/tmp/codex-screenshots/apps-unify/relay-mark-contacts.png`, `relay-voxel-new.png`,
`relay-2.4.0-panel.png`, `relay-2.4.0-testbed.png`, `relay-2.4.0-social.png`, `relay-page-390.png`.

Open: the 390 px pass was rendered by headless Chrome without device emulation, and the committed 2.3.9 page
renders the same way, so the hero headline width needs a real mobile check in the next wave.
