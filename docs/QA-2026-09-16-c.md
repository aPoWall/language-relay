# QA · Language Relay 2.4.1 build 21 · 2026-09-16 · control walk

Wave 7 (`SPRINT-2026-09-16-SHELL.md` § C), rule 41: every control of the product walked as one table,
control -> press -> what changed. Everything ran in the background through the app's own `--testbed` route and
`testbed/bin/axdrive`. No synthetic click, no `activate`; the cursor was read (0.0,0.0) and never moved, the
frontmost app stayed Finder before and after every press.

Panel under test: window 7826, popover 446 x 521 with the shadow, 33 AX nodes, content 420 x 488.
Stored values are read back from `defaults read dev.alex.layout-pilot`. Every default the walk moved was put
back to the value it was found at (`fixMode phrase`, `capitalizationMode preserve`, `shiftEnabled 1`,
`optionEnabled 1`, `soundLevel quiet`, `pinned 1`).

## Header

| control | press | what changed |
|---|---|---|
| `product-mark` (AXImage, 40 pt) | not pressable, rule 39 illustration | header mark = menu bar drawing, `AXImage desc="language relay"` |
| name + version line | static | `AXStaticText id="product-version" value="2.4.1 · build 21"` under the name (rule 21) |
| `settings` | `press "id:settings"` | NSMenu window 7827 153 x 93 opens (setup details, switch layout, quit); no AX reply, the menu is a separate window. It leaves with the panel since build 21 and does not return on the next show |
| `pin-panel` | `press "id:pin-panel"` | `unpin panel` -> `pin panel open`, glyph ◉ -> ○, `dev.alex.layout-pilot.pinned` 1 -> 0; second press restores both |
| `close-panel` | `press "id:close-panel"` | the panel leaves through `close(reason: .closeButton)`, both windows offscreen, frontmost unchanged |

## Body

| control | press | what changed |
|---|---|---|
| `layout-switch` ⇄ | `press "id:layout-switch"` | readout `a ⇄ [ру] · russian – pc` -> `[a] ⇄ ру · u.s.`; the input source is system state, not a stored default; second press restores it |
| `last word` | `press "id:last word"` | `last word` -> `last word, selected` (AXValue 0 -> 1) and `last phrase, selected` -> `last phrase`; `fixMode` phrase -> lastWord |
| `last phrase` | `press "id:last phrase"` | the mirror of the row above; `fixMode` lastWord -> phrase |
| `aA preserve` · `Aa sentence` · `AA upper` · `aa lower` | one press each | the pressed item takes `, selected` and AXValue 1, the previous one drops both; `capitalizationMode` preserve -> sentence -> uppercase -> lowercase -> preserve |
| `gesture-shift` ⇧⇧ | `press "id:gesture-shift"` | footer keys `⇧⇧ ⌥ · repair` -> `⌥ · repair`; `shiftEnabled` 1 -> 0; second press restores both |
| `gesture-option` ⌥ | `press "id:gesture-option"` | footer keys `⇧⇧ ⌥ · repair` -> `⇧⇧ · repair`; `optionEnabled` 1 -> 0; second press restores both |
| `sound` (cue ▾) | `press "id:sound"` | NSMenu window 7828 101 x 152 with the eight cues; no AX reply, the menu leaves with the panel (build 21) |
| `00` · `25` · `55` · `82` | one press each | the pressed level takes `, selected` and AXValue 1; `soundLevel` quiet -> silent -> quiet -> balanced -> quiet -> full -> quiet; the cue caption follows: `cue · pulse · ▾` -> `cue · muted · ▾` on `00` |
| `setup` | `press "id:setup"` | the button is replaced by the disclosure: `ready · hammerspoon bridge · last ready` plus a `hide` button; second press folds it back. Session state, no stored default (rule 38: the view changes, the key stays) |

## Footer and keys

| control | press | what changed |
|---|---|---|
| `hint-keys` · `hint-esc` · `hint-status` | static, three nodes | `⇧⇧ ⌥ · repair` · `esc close` · `v2.4.1 · bridge · ready`; the keys node follows the two gesture toggles above |
| Escape, Command-W | `axdrive key --keys esc` / `cmd-w` | nothing: a testbed panel never becomes key, so posted keys do not reach it (`testbed/README.md` § Known limits). Both close the panel when the user has it focused; the testbed closes through `hide` or `×` |
| menu bar item, hotkey | user surfaces, not driven | both end in the same `close(reason:)` (rule 33) |

## Wave-7 checks around the walk

| # | check | how | result |
|---|---|---|---|
| 1 | header version (rule 21) | `--render-ui`, then capture of window 7826 | `2.4.1 · build 21` on the status line under the name, nothing truncated; the right-edge slot of rule 32 stays empty on a 388 pt row and the order settings · pin · x holds |
| 2 | idempotent `--testbed show` (rule 33) | three consecutive shows, then three hide/show cycles | window count flat: 2 on screen, ids unchanged (7825 anchor, 7826 panel); before the fix each show and each cycle added one allocated window |
| 3 | segment state in Accessibility (rule 31) | `press` on an active item and on an inactive one | an active item reads `…, selected` with AXValue 1, so a press on the already active scope still reports the state a driver can read |
| 4 | app tests | `make test` | 16 conversion tests pass; UI self-test pass, now asserting the header version line and the segment names |
| 5 | testbed pass | `check.sh run relay` | `PASS · window 7809 · 33 AX nodes · after hide: gone · frontmost unchanged "Finder" · cursor unchanged` |
| 6 | install | `release-app.mjs relay --install` (`install-runtime.sh update-background`) | installed 2.4.1 build 21, rollback snapshot `20260916T205042Z` |
| 7 | text | no U+2014 in the sources, docs or the product page | pass |

Screenshots: `/tmp/codex-screenshots/apps-shell/` (`hdr-relay-2.4.1b18.png` header, `relay-panel-b21.png`,
`relay-settings-menu.png`, `relay-cue-menu.png`, `relay-header-offscreen.png`).

Open: the panel shifts 7 pt when the content is rebuilt after a press, because the popover re-anchors on the new
content; it is visible as a frame change in consecutive AX trees and is worth one wave of its own.
